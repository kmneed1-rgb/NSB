rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all collections for now to ensure syncing works.
    // In a production app, we would add Authentication and per-user rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
